# OpenSpec Configuration — rendimiento-saas
# Auto-generated by sdd-init on 2026-06-10

context: |
  Python 3 repair workshop management app (NSP Notebooks).
  Pure stdlib http.server + sqlite3 backend. Vanilla HTML/CSS/JS frontend.
  Single-file server.py (~1600 loc), index.html SPA, placas-data.js inventory.
  SQLite database with 15+ tables: orders, repairs, boards, ICs, measurements, solutions, references.
  No framework, no build step, no package manager.
  Optional: fpdf (PDF reports), requests + bs4 (datasheet scraping).

strict_tdd: false

testing:
  test_runner: null
  test_layers:
    unit: false
    integration: false
    e2e: false
  coverage: null
  linter: null
  type_checker: null
  formatter: null

phases:
  proposal: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
